# AMS stub

Prebuilt service binary-equivalent — treat it like a third-party API you don't control.

```bash
node server.js     # listens on http://localhost:8472
```

No install step. Requires Node 18+. Do not modify or reverse-engineer your way around it — your solution is run against a fresh instance on our side.
